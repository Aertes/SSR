import styles from './dashboard.module.css';
export default function Dashboard() {
    return(
        <div className={`${styles.dashboard_container} container`} >
            Dashboard
        </div>
    )
}